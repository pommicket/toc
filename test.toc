#include "std/mem.toc", mem;
#include "std/io.toc", io;
main ::= fn() {
	i := mem.new(int);
	*i = 3;
	io.puti(*i);
	ns := mem.news(int, 10);
	for n, i := &ns {
		*n = i;
	}
	for n := ns {
		io.puti(n);
	}
	mem.del(i);
	mem.dels(ns);
}
main();

