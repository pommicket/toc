#C("#include <stdio.h>\n");

print_int @= fn(y: int) {
		#C("printf(\"%ld\\n\", (long)x);\n");
};

times2 @= fn(x: int) int { x + x };

main @= fn() {
	 print_int(times2(5));
	 foo := fn (x, y, z : f32, A:f64) {};
};
