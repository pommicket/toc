arr ::= pkg "std/arr";
io ::= pkg "std/io";
main ::= fn() {
#include "incstuff.toc";

	s : [1]char;
	s[0] = (arr.arr_len(x) as char) + '0';
	io.puts(s[:]);
	for c := x.data {
		s[0] = c;
		io.puts(s[:]);
	}
};