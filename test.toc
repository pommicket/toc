#C("#include <stdio.h>
#define kasfdhkjasdfhjk ");

puti @= fn(x: int) {
	 #C("printf(\"%ld\\n\", (long)x)");
};

main @= fn() {
	 N := 10;
	 numbers := new(int, N);
	 i := 0;
	 while i < N {
	 	   numbers[i] = i;
		   i = i + 1;
	 }
	 puti(numbers[:8][0]);
};
