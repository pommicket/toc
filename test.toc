#include "std/io.toc";

f ::= fn(x :: ..) int {
  total := 0;
  for _, i := x {
  	  total += i * (x[0] as int);
  }
  total
};


main ::= fn() {
	 puti(f(5));
	 
	 puti(f(5,6));
	 
	 puti(f(1,2,3));
	 
	 puti(f(1,1,1,1));
	 
};

main();