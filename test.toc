#C("#include <stdio.h>
#define kasfdhkjasdfhjk ");

puti @= fn(x: int) {
	 #C("printf(\"%ld\\n\", (long)x)");
};

foo @= fn() int {
	 N := 10;
	 numbers := new(int, N);
	 i := 0;
	 while i < N {
	 	   numbers[i] = i;
	 	   i = i + 1;
	 }
	 numbers[5:7][1] + numbers[2:][0] + numbers[:6][3]
};

main @= fn() {
	 x : [foo()]int;
	 puti(foo());
};
