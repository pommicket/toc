#include "std/io.toc";

g ::= fn(x : ..) int {
  total := 0;
  for e, i := x {
  	  total += i * (e as int);
  }
  total
};

f ::= fn(x : ..) int {
  g(x)
};

main ::= fn() {
	 puti(g(5));
	 
	 puti(f(5,6));
	 
	 puti(f(1,2,3));
	 
	 puti(f(1,1,1,1));
	 
};

main();