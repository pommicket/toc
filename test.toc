#C("#include <stdio.h>\n");

print_int @= fn(x: int) {
		#C("printf(\"%ld\\n\", (long)x);\n");
};

times2 @= fn(x: int) int { 5 /* x*/ };

main @= fn() {
	 print_int(times2(5));
	 foo := fn (x, y, z : float, y:double) {};
};
