#include "std/io.toc", io;

bar ::= fn() (int, int, int) {
	return foo();
}
foo ::= fn() (int, int, int) {
	return -9223372036854775808, 32, 0;
}

main ::= fn() {
	a, b, c := foo();
	io.puti(a);
	io.puti(b);
	io.puti(c);
}
