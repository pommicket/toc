#include "std/io.toc";
/*
sum ::= fn(n::=, t::=, a: [n]t) t {
	total : t = 0;
	for x := a {
		total += x;
	}
	total
}

main ::= fn() {
	t ::= int;
	numbers : [1000]t;
	for x, i : (&t, t) = &numbers {
		*x = i;
	}
	puti(sum(numbers) as int);
}
*/
Point ::= struct {
	x, y : int;
}

main ::= fn() {
	p: Point;
	p.x = 12;
}
